{
  "name": "liyx-news",
  "version": "1.0.0",
  "description": "LIYX — live world news feed, wrapped for Android with AdMob.",
  "author": "Liyog Bartoos O.",
  "license": "MIT",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build-debug": "cd android && ./gradlew assembleDebug",
    "build-release": "cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor-community/admob": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}

